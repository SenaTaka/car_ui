# next_plan

- [x] 差分を機能群で確定する
  - フリーズフレーム / レディネス / トリップ / 燃費 / メーカー別プロファイル / HUD / 複数ダッシュボードのうち、実装対象を優先順位付きで確定
  - Car Scanner の掲載機能から、必須 / あると強い / 後回し を分ける
  - 既存の `car_ui` で代替済みの項目は除外して、重複実装を避ける

- [x] 既存実装の拡張点を洗い出す
  - `PIDCatalog` / `DashboardView` / `SensorsView` / `ChartsView` / `ToolsView` / `TelemetryRecorder` のどこを拡張するか決める
  - 新規データモデルが必要か、既存の `liveValues` / `recorder` で足りるか判定
  - 車両依存機能は BLE ELM327 で取得可能か、手動コマンド前提かを切り分ける

- [x] 画面設計を先に固める
  - 新しいタブやセクション追加が必要なら、どの画面に置くか決める
  - 一覧 / 詳細 / 診断 / 記録 の導線を決める
  - ユーザー操作が多い機能は、最小操作数になる UI にする

- [x] 実装する
  - 不足機能を小さい単位で追加
  - 既存のポーリングや記録の仕組みを再利用
  - 車両への送信コマンドは読み取り専用を維持し、消去や書き込みは明示操作に限定

- [x] 検証する
  - `xcodebuild` でコンパイル確認
  - デモモードやシミュレータで UI 崩れを確認
  - 追加した PID / 診断 / 表示が既存機能を壊していないか確認

- [x] 記録を残す
  - `done.md` に作業ログを追記
  - 必要なら `CLAUDE.md` / `AI_AGENT_NOTES.md` に再利用可能な知見を昇格
  - 次回の作業で迷わないよう、未完了項目と注意点を明記
